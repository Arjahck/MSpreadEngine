services:
  mspread-engine:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: python main.py run --host 0.0.0.0 --port 8000 --reload
